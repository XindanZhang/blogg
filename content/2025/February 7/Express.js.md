RESTful API example, routing using Express.js Router, and incorporating middleware

In the `server.js` file
```javascript
const express = require('express');
const app = express();
const port = 3000;


```
